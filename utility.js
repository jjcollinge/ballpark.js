/**
 * Utility functions
 */
 
module.exports.each = function(list, callback) {
    list.forEach(callback);
}

modules.exports.reduce = function(list, callback) {
    list.reduce(callback);
}

modules.exports.map = function(list, callback) {
    list.map(callback);
}